# Dependencies & build output — rebuilt inside the image
node_modules
.next
out
build

# Prisma client is regenerated by `npm ci` (postinstall) in the builder
src/generated

# Local env & secrets — provided by the platform at runtime
.env
.env.*
!.env.example

# VCS & tooling
.git
.gitignore
.github

# Editor / OS noise
.DS_Store
*.pem
npm-debug.log*

# Docs & deploy config not needed in the image
DEPLOYMENT.md
.do
README.md
