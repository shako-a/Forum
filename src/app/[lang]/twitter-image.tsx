// X/Twitter reads `twitter:image` rather than `og:image`. Same card — this
// file exists only so Next emits the twitter:* tags alongside the og:* ones.
export { default, alt, size, contentType, generateStaticParams } from "./opengraph-image";
